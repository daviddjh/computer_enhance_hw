mov SI, BX
mov DH, AL
mov CL, 12
mov CH, 65524
mov CX, 12
mov CX, 65524
mov DX, 3948
mov DX, 61588
mov AL, [BX + SI]
mov BX, [BP + DI]
mov DX, [BP + 0]
mov AH, [BX + SI + 4]
mov AL, [BX + SI + 4999]
mov [BX + DI], CX
mov [BP + SI], CL
mov [BP + 0], CH
